query User($username: String!) {
  user(login: $username) {
    avatarUrl
    name
    status {
      emojiHTML
      message
    }
    url
    login
    bioHTML
    company
    location
    email
    websiteUrl
    twitterUsername
  }
}
